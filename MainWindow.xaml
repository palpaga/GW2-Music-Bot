<Window x:Class="Gw2MusicBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Gw2MusicBot"
        Title="GW2 Music Bot - API Edition" Height="920" MinHeight="900" Width="680" MinWidth="680" Topmost="True" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#E0E0E0" FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    
    <Window.Resources>
        <local:FavoriteColorConverter x:Key="FavColorConv"/>

        <!-- Modern ScrollBar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#555555"/>
        </Style>

        <!-- Modern Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#2D2D30"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Accent Button (Blue) -->
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0098FF"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Button (Play) -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Stop Button -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C62828"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                                <Setter Property="Background" Value="#1E1E1E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer Padding="5">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <!-- Expander -->
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="14" Foreground="#007ACC" Margin="5,0"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GroupBox -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Grid.RowSpan="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Margin="0,15,0,0"/>
                            <Border Grid.Row="0" Background="#1E1E1E" HorizontalAlignment="Left" Margin="20,0,0,0" Padding="10,0">
                                <TextBlock Text="{TemplateBinding Header}" FontSize="15" FontWeight="SemiBold" Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Slider -->
        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Name="TxtMainTitle" Text="GW2 Music Bot" FontSize="22" FontWeight="SemiBold" Foreground="#007ACC" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="🌐" FontSize="14" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="#A0A0A0"/>
                <ComboBox Name="CmbLanguage" Width="60" SelectionChanged="CmbLanguage_SelectionChanged" Padding="5,2" Background="#2D2D30" Foreground="Black">
                    <ComboBoxItem Content="EN" Tag="en"/>
                    <ComboBoxItem Content="FR" Tag="fr"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="TxtSearch" Grid.Column="0" VerticalContentAlignment="Center" FontSize="12" Padding="8,8" Text="Zelda"/>
            <Button Name="BtnSearch" Grid.Column="1" Content="🔍 Search" FontSize="12" Padding="15,8" Margin="10,0,0,0" Click="BtnSearch_Click"/>
            <Button Name="BtnShowFavorites" Grid.Column="2" Content="★ Favorites" FontSize="12" Padding="15,8" Margin="10,0,0,0" Click="BtnShowFavorites_Click"/>
            <Button Name="BtnLoadLocal" Grid.Column="3" Content="📂 Local..." FontSize="12" Padding="15,8" Margin="10,0,0,0" Click="BtnLoad_Click"/>
        </Grid>

        <!-- Results List -->
        <ListBox Name="LstResults" Grid.Row="2" Margin="0,0,0,10" SelectionChanged="LstResults_SelectionChanged" ScrollViewer.VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch" MinHeight="200" Padding="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="★" MinWidth="35" MinHeight="35" Padding="5,0" Margin="0,0,10,0" Click="BtnToggleFavorite_Click" Tag="{Binding}" Background="Transparent" FontSize="18" ToolTip="Toggle Favorite" Foreground="{Binding Checksum, Converter={StaticResource FavColorConv}}"/>

                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" FontSize="13" FontWeight="Medium" TextTrimming="CharacterEllipsis"/>
                        
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                            <TextBlock Text="Score: " Foreground="#A0A0A0" FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Score}" Foreground="#4CAF50" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Settings -->
        <GroupBox Name="GrpPlayerSettings" Grid.Row="3" Header="Player Settings" Margin="0,0,0,10">
            <StackPanel Margin="0,5,0,0">
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Name="TxtMidiTrackLabel" Grid.Column="0" Text="MIDI Track:" VerticalAlignment="Center" Foreground="#B0B0B0" Margin="0,0,10,0" FontSize="12"/>
                    <ComboBox Name="CmbTracks" Grid.Column="1" FontSize="12" SelectionChanged="CmbTracks_SelectionChanged" Margin="0,0,10,0" Padding="5,4" Background="#2D2D30" Foreground="Black"/>
                    <Button Name="BtnPreviewTrack" Grid.Column="2" Content="▶ Preview Track" FontSize="12" Padding="15,5" Click="BtnPreviewTrack_Click" IsEnabled="False" Style="{StaticResource AccentButton}"/>
                </Grid>

                <StackPanel Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox Name="ChkTwoOctaves" Content="Restrict to 2 Octaves" FontWeight="SemiBold" FontSize="12" Checked="ChkTwoOctaves_Changed" Unchecked="ChkTwoOctaves_Changed"/>
                        <TextBlock Name="TxtOctaveDelayLabel" Text=" | Octave Delay (ms):" VerticalAlignment="Center" FontSize="12" Foreground="#E0E0E0" FontWeight="SemiBold" Margin="10,0,10,0"/>
                        <TextBox Name="TxtOctaveDelay" Width="40" TextAlignment="Center" VerticalContentAlignment="Center" TextChanged="TxtOctaveDelay_TextChanged" FontSize="12"/>
                    </StackPanel>
                    <TextBlock Name="TxtTwoOctavesDesc" Text="Reduces octave switching to prevent dropped notes in fast songs." FontSize="11" Foreground="#808080" Margin="25,0,0,0" TextWrapping="Wrap"/>
                </StackPanel>

                <Grid Margin="0,5,0,2">
                    <TextBlock Name="TxtPlaybackSpeedLabel" Text="Playback Speed:" HorizontalAlignment="Left" Foreground="#B0B0B0" FontSize="12"/>
                    <TextBlock Name="TxtSpeedVal" Text="1.00x" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#007ACC" FontSize="12"/>
                </Grid>
                <Slider Name="SliderSpeed" Minimum="0.1" Maximum="2.0" Value="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" ValueChanged="SliderSpeed_ValueChanged" Margin="0,0,0,5"/>
                
                <Expander Name="ExpKeyMapping" Header="Key Mapping" Margin="0,5,0,0">
                    <Border BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="4" Padding="10" Background="#1E1E1E" Margin="0,5,0,0">
                            <StackPanel>
                                <CheckBox Name="ChkDisableFKeys" Content="Disable Sharp/Flat notes (Ignore F1-F5)" FontWeight="Medium" Checked="ChkDisableFKeys_Changed" Unchecked="ChkDisableFKeys_Changed" Margin="0,0,0,2"/>
                                <TextBlock Name="TxtDisableFKeysDesc" Text="Function keys are only for Piano. Keep checked for other instruments." FontSize="12" Foreground="#808080" Margin="25,0,0,20" TextWrapping="Wrap"/>
                                
                                <!-- KeyBind Grid -->
                                <Grid>
                                    <Grid.Resources>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                            <Setter Property="Background" Value="#2D2D30"/>
                                            <Setter Property="Foreground" Value="#007ACC"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Padding" Value="4,6"/>
                                            <Setter Property="FontSize" Value="12"/>
                                        </Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#B0B0B0"/>
                                            <Setter Property="FontSize" Value="11"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="0,0,0,4"/>
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Top Row (Sharps) -->
                                    <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="Do#"/>
                                        <TextBox Name="TxtNote1" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_1" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="Ré#"/>
                                        <TextBox Name="TxtNote3" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_3" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="3" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="Fa#"/>
                                        <TextBox Name="TxtNote6" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_6" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="4" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="Sol#"/>
                                        <TextBox Name="TxtNote8" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_8" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="5" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="La#"/>
                                        <TextBox Name="TxtNote10" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_10" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Grid.Row="0" Grid.Column="6" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="Oct -" Foreground="#4CAF50"/>
                                        <TextBox Name="TxtOctDown" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="OctaveDown" IsReadOnly="True" TextAlignment="Center" Foreground="#4CAF50"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="7" HorizontalAlignment="Right" Margin="0,0,-20,15" Panel.ZIndex="1">
                                        <TextBlock Text="Oct +" Foreground="#4CAF50"/>
                                        <TextBox Name="TxtOctUp" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="OctaveUp" IsReadOnly="True" TextAlignment="Center" Foreground="#4CAF50"/>
                                    </StackPanel>

                                    <!-- Bottom Row (Naturals) -->
                                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="2">
                                        <TextBlock Text="Do" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote0" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_0" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="2">
                                        <TextBlock Text="Ré" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote2" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_2" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="2">
                                        <TextBlock Text="Mi" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote4" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_4" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="3" Margin="2">
                                        <TextBlock Text="Fa" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote5" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_5" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="4" Margin="2">
                                        <TextBlock Text="Sol" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote7" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_7" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="5" Margin="2">
                                        <TextBlock Text="La" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote9" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_9" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="6" Margin="2">
                                        <TextBlock Text="Si" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtNote11" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="Note_11" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="7" Margin="2">
                                        <TextBlock Text="Do2" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        <TextBox Name="TxtHighC" Width="36" PreviewKeyDown="KeyBind_KeyDown" Tag="HighC" IsReadOnly="True" TextAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.Column="8" Margin="2" Grid.ColumnSpan="2">
                                        <TextBlock Text="Stop" Foreground="#E53935" FontWeight="Bold"/>
                                        <TextBox Name="TxtStop" Width="76" PreviewKeyDown="KeyBind_KeyDown" Tag="Stop" IsReadOnly="True" TextAlignment="Center" Foreground="#E53935"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Expander>
                </StackPanel>
        </GroupBox>

        <!-- Footer / Playback -->
        <StackPanel Grid.Row="4">
            <TextBlock Name="TxtFileName" Text="No music selected" FontStyle="Italic" Foreground="#007ACC" HorizontalAlignment="Center" FontSize="14" Margin="0,0,0,10"/>
            
            <Grid Margin="0,0,0,10">
                <Button Name="BtnPlay" Style="{StaticResource PrimaryButton}" Content="▶ PLAY" Padding="15" Click="BtnPlay_Click" IsEnabled="False" FontSize="18" HorizontalAlignment="Stretch"/>
            </Grid>
            
            <Border Background="#2D2D30" BorderBrush="#E53935" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,5,0,15">
                <StackPanel>
                    <TextBlock Name="TxtDisclaimerTitle" Text="⚠️ DISCLAIMER &amp; ARENANET POLICY" Foreground="#E53935" FontWeight="Bold" FontSize="11" Margin="0,0,0,6"/>
                    <TextBlock Foreground="#CCCCCC" FontSize="11" TextWrapping="Wrap" LineHeight="16">
                        <Run Name="RunDisclaimerText1" Text="While using macros specifically for playing musical instruments has historically been tolerated by ArenaNet, "/><Run Name="RunDisclaimerBold" Text="you use this tool entirely at your own risk." FontWeight="Bold" Foreground="#FFA726"/><Run Name="RunDisclaimerEnd" Text=" It is a third-party macro program. We hold no responsibility for any actions taken against your account."/><LineBreak/>
                        <Hyperlink NavigateUri="https://help.guildwars2.com/hc/en-us/articles/360013762153-Policy-Macros-and-Macro-Use" RequestNavigate="Hyperlink_RequestNavigate" TextDecorations="Underline" Foreground="#007ACC" ToolTip="https://help.guildwars2.com/hc/en-us/articles/360013762153-Policy-Macros-and-Macro-Use">
                            <Run Name="RunDisclaimerLink" Text="Read official ArenaNet Macro Policy" Cursor="Hand" />
                        </Hyperlink><LineBreak/>
                        <Run Name="RunDisclaimerLimitations" Text="Limitations: The bot presses keys blindly with no game feedback. Input lag can cause missed keys/octaves. Adjust speed, track, or octave delay if the sound degrades." Foreground="#AAAAAA" FontStyle="Italic"/>
                    </TextBlock>
                </StackPanel>
            </Border>
            
            <TextBlock HorizontalAlignment="Center" FontSize="11" Foreground="#666666">
                Made with ❤️ by Lama and Google Gemini -
                <Hyperlink NavigateUri="https://github.com/palpaga/GW2-Music-Bot" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#007ACC">
                    GitHub
                </Hyperlink>
            </TextBlock>
            
            <TextBlock Name="TxtUpdate" Margin="0,10,0,0" HorizontalAlignment="Center" FontSize="13" Visibility="Collapsed">
                <Hyperlink Name="LinkUpdate" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#4CAF50" TextDecorations="None">
                    <Run Name="RunUpdateText" Text="✨ New version available! Click to update." FontWeight="Bold"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
